// kind:expr validate:false pass:true ignore:false file:'ddlog-syntax/src/parser/expr.rs' line:194 column:3
{
    x += y;
    x -= y;
    x /= y;
    x *= y;
    x %= y;
    x ^= y;
    x &= y;
    x |= y;
}
