// kind:item validate:false pass:true ignore:false
fn fibonacci(n: usize) -> usize {
    if n == 0 or n == 1 {
        return n;
    } else {
        // TODO: Function calls
        fibonacci
    }
}
