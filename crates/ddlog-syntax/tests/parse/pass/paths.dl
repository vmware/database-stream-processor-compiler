// kind:item validate:false pass:true ignore:false
fn main() {
    let edges = input("edges");
    let paths = edges.iterative(|edges| {
        edges
            .join(edges.reverse())
            .distinct()
    });
    output(paths, "paths");
}
